# ================================
# Default Region (Optional)
# Users can override per request
# ================================
SPACES_REGION=sgp1
SPACES_ENDPOINT=https://sgp1.digitaloceanspaces.com

# ================================
# AI Agent (OpenAI-compatible)
# ================================
DO_AGENT_URL=https://your-agent-host/v1/chat/completions
DO_AGENT_KEY=your_agent_api_key

# ================================
# App Security (optional but recommended)
# ================================
APP_API_KEY=devkey

# ================================
# NOTE: Multi-Tenant Configuration
# ================================
# DigitalOcean Spaces credentials are NO LONGER configured globally.
# Users must provide their access key, secret key, log bucket, and 
# metrics bucket with each API request.
#
# This allows multi-tenant usage where each request uses its own
# credentials and accesses only its own resources.
#
# Example request headers:
#   X-Spaces-Key: your_spaces_access_key
#   X-Spaces-Secret: your_spaces_secret_key
#   X-Log-Bucket: my-access-logs
#   X-Metrics-Bucket: my-metrics-bucket
# ================================

